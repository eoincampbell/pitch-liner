# Minor Improvements

Make the color of the Share Button purple.

Allow the user to click on the name of the path in the stats table to rename it. The renaming should be reflecting the shareurl and csv files also.
Remove the Lat/Long from the display table on the screen to allow this table to be narrower but keep them in the CSV file and share url for functional reasons
Allow the buttons in the top corner to be arranged on multple rows for width space saving.
Add a "minimize" chevron button to allow someone to collapse the table (but it should always be open by default when the page loads).
